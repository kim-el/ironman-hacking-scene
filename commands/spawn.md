# /spawn — enable dynamic TUI task monitors

Toggles on the status panel hook. When enabled, every code change spawns a small kitty monitor window showing what's happening — apt-get style, window-aware formatting, auto-debounced.

Re-run `/spawn` to toggle off. Use `/cancel` to kill all active panels immediately.
